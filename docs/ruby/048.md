# How to run terminal command

We can use [system](https://apidock.com/ruby/v2_5_5/Kernel/system), but better way is to use `open3` because it has awesome interface for STDIN/STDOUT

```ruby
require 'open3'

stdout, stderr, status = Open3.capture3("<SCRIPT>", stdin_data: YOUR_STDIN)
```
