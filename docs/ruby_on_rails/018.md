# How to calculate number of workers for puma


```ruby
if ENV['RAILS_ENV'] == 'production'
  require 'concurrent-ruby'
  worker_count = Integer(Concurrent.physical_processor_count) # It's max number
  workers worker_count if worker_count > 1
end
```
